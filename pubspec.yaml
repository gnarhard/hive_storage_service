name: hive_storage_service
description: A service for managing storage with Hive.
version: 1.0.0

environment:
  sdk: '>=2.18.6 <3.0.0'

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  json_serializable: ^6.6.0
  mockito: ^5.3.2
  json_annotation: ^4.8.0
  build_runner: ^2.3.3
  hive_generator: ^2.0.0
  http_cache:
    git:
      url: https://github.com/gnarhard/http_cache.git
      ref: main
dependencies:
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  path_provider: ^2.0.12
  flutter:
    sdk: flutter
